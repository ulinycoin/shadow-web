# Core SDK (matches pyproject.toml dependencies)
lxml>=5.1.0
playwright>=1.42.0
requests>=2.31.0
pydantic>=2.6.0
aiohttp>=3.9.0

# Optional — install with: pip install -e ".[server,mcp,dev]"
# fastapi>=0.110.0
# uvicorn>=0.28.0
# openai>=1.14.0
# mcp>=1.2.0
